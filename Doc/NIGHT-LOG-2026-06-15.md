# 夜间自主作业日志 — 2026-06-15 → 06-16

> 目标(用户设,5 点前):做出能显示 HTML、能访问网页的最小版本。
> 诚实预期:完整可联网渲染引擎一晚做不出(WebCore 还没编过、网络栈未选型)。
> 今晚交付:把依赖栈打穿 + WebCore 接线/configure + 尽量深推编译,部署上屏那步等用户醒。

## 字体栈决策(已定)
走 **A:FreeType + Fontconfig + HarfBuzz(Cairo 软渲染)**,用 `FontPlatformDataFreeType.cpp`(GTK/WPE 后端)。
推翻 HANDOFF 的 DirectWrite(WinCairo 后端是 GDI/Uniscribe 绑定 = App Container 死路);
证实 FreeType 路零 GLib。详见记忆 `phase1-plan`。

## 进度

### ✅ S1. fontconfig 2.17.1 (arm-uwp) — 完成(头号风险项已过)
- 产物:`C:\vcpkg\installed\arm-uwp\lib\{fontconfig.lib, pkgconfig\fontconfig.pc}` + `fontconfig-1.dll` + `include\fontconfig\` + `etc\fonts\`(fonts.conf + conf.d 全套)。
- 脚本:`port\build-fontconfig.ps1`;cross-file:`port\fontconfig-cross-clang.txt`(= cairo 的但**去掉 -DUNICODE/-D_UNICODE**,因 fontconfig Win 代码走 ANSI 路径 API)。
- App Container 源码补丁(在 `C:\vcpkg\buildtrees\fontconfig\src\2.17.1-6edd6fe145.clean\src\fcxml.c`,volatile,需固化成 .patch):
  1. 两处 `_mbsrchr(path,'\\')` → `(FcChar8*)strrchr((char*)path,'\\')`(UCRT 在 WINAPI_FAMILY_APP 下禁用 MBCS `_mbs*` 函数;包安装路径是 ASCII,strrchr 安全)。
  2. `_ensureWin32GettersReady` 里 `GetWindowsDirectory` 桌面回退 → 用 `#if WINAPI_FAMILY_PARTITION(WINAPI_PARTITION_DESKTOP)` 守卫,App Container 分支置 NULL(系统字体目录沙箱读不到,改用打包字体)。
- 构建要点(复用 §8 配方):自己用 clang-cl cross-file 重新 `meson setup`(vcpkg 那次用 v141 cl 无 C11);patch `build.ninja` 的 `/MACHINE:thumbv7`→`/MACHINE:ARM`(2 处);gperf 用 vcpkg host 版 `installed\x64-windows\tools\gperf`;`fcobjshash.h` 架构无关已复用。meson 选项:`-Dxml-backend=expat -Dcache-build=disabled -Dtools=disabled -Dtests=disabled -Ddoc*=disabled -Dnls=disabled -Diconv=disabled`。
- 运行时待验(真机):FcInit 的 fonts.conf/cache 路径要指向包内 + LocalState(沙箱可写),早期单独 harness 验。

### ✅ S2. cairo 重编(+freetype+fontconfig) — 完成
- `build-cairo.ps1` 翻 `-Dfreetype=enabled -Dfontconfig=enabled`(保持 `-Ddwrite=disabled` image-only)。
- 验证:`cairo-ft.h` 已装、`CAIRO_HAS_FT_FONT/FC_FONT/USER_FONT` 全 1、`cairo.lib` 含 `cairo_ft_font_face_create_for_pattern/_for_ft_face/_scaled_font_lock_face`(T)。FreeType 后端硬依赖已可链。
- App Container 补丁:`cairo-ft-font.c` 的 `#if HAVE_UNISTD_H ... #elif !defined(access)` 块改成 Windows 强制 `#undef access` + `#define access(p,m) 0`(UCRT 在 App Container 下 `_access` 受限;跳过可读性预检,交给 FT_New_Face 报错)。

### ✅ S3. harfbuzz 14.2.1 — 完成
- `build-harfbuzz.ps1` + `harfbuzz-cross-clang.txt`(= cairo 的但 **`-EHsc`→`-EHs-c-` + `-D_HAS_EXCEPTIONS=0`**,因 harfbuzz 是 C++,clang thumbv7 无法 lower cleanupret)。
- 选项:`-Dfreetype=enabled -Dicu=disabled -Dglib/gobject/cairo/png/zlib/tests/utilities/docs/gpu/subset=disabled`;gdi/directwrite/coretext 默认已 disabled。
- 产物:`harfbuzz.lib`(140KB)+ `.pc` + `hb.h`;含 `hb_ft_face_create_cached`(T)。
- **ICU 未编(无 triplet ICU)**:WebKit 的 `HarfBuzz::ICU` 接线要在 WebCore 阶段处理——要么给 C:\icu-arm-uwp 造 `icu-uc.pc` 重编 harfbuzz 带 icu,要么 patch `FreeType.cmake` 去掉 `HarfBuzz::ICU` + 让 `ComplexTextControllerHarfBuzz` 用 WTF 的 ICU(`hb_icu_script_to_script` 改手写 UScript→hb_script)。

### 🔑 关键补丁:freetype.h 的 `generic` 改名(影响所有 FreeType 客户端,含将来 WebKit)
- `C:\vcpkg\installed\arm-uwp\include\freetype\freetype.h:1230`:vcpkg 的 UWP 补丁在 `#if defined(WINAPI_FAMILY) && (WINAPI_FAMILY != WINAPI_FAMILY_DESKTOP_APP)` 下 `#define generic GenericFromFreeTypeLibrary`(为躲 C++/CX `generic` 关键字),把 `FT_FaceRec_::generic` 等成员**无条件**改名 → harfbuzz/WebKit 用 `ft_face->generic` 全编不过。
- 修:守卫收窄成 `#if defined(__cplusplus_winrt)`(只有真用 /ZW 的 C++/CX TU 才改名;我们全用不带 /ZW 的 clang-cl,`generic` 不是关键字)。这是根因修,一处修好所有客户端。**注:改的是 installed 头(freetype 不会重编,安全),但需固化成 vcpkg port patch 以防重装。**

### ✅ S4 sqlite3 · libjpeg-turbo · libwebp · libxml2 — 全部完成
- **sqlite3**:手编 amalgamation,`-DSQLITE_API=`(静态空导出)`-DSQLITE_OS_WINRT=1 -DSQLITE_OMIT_SEH=1`(clang thumbv7 无 SEH),并在 syscall 表 null 掉 5 个 desktop API(`AreFileApisANSI/CreateFileA/CreateFileW/GetFileSize/HeapValidate`,WinRT release 路径不调)。产物 `sqlite3.lib`(1.8MB)。补丁在 vcpkg buildtree 的 sqlite3.c(需固化)。
- **libjpeg-turbo 3.1.4 / libwebp 1.6.0 / libxml2 2.15.3**:**vcpkg 直接编出来了!**(发现 vcpkg 能编 CMake-based 的 arm-uwp 端口,只有 autotools/meson 端口不行)。libxml2 用 `[core,zlib]` 甩掉 autotools 的 libiconv 依赖。

### ✅ S6 WebCore CMake 接线 — 完成并 configure 通过
- 多智能体 workflow 起草 `Source/WebCore/PlatformWinUWP.cmake`(从 PlatformWin.cmake 裁出 App Container 安全子集:去 MSAA/剪贴板/拖拽/原生 select/全屏/光标/GDI 图像/MediaFoundation/EGL-ANGLE/Uniscribe+GDI 字体后端,换成 `include(FreeType.cmake)`+`Cairo.cmake`)。已应用,**网络相关(Curl/OpenSSL/CurlSSLHandleWin)注释延后**。
- `OptionsWinUWP.cmake`:`ENABLE_WEBCORE ON` + 最小特性集(媒体/GPU/桌面外壳/XSLT/MathML 等全关)+ USE 后端(Cairo/FreeType/Fontconfig/HarfBuzz ON;Skia/TextureMapper/ANGLE/MediaFoundation/Curl/OpenSSL/LCMS/JPEGXL/AVIF/WOFF2 OFF)+ 依赖 find_package。
- `FreeType.cmake` 删 `HarfBuzz::ICU`(harfbuzz 未编 hb-icu;`ComplexTextControllerHarfBuzz.cpp:377` 的 `hb_icu_script_to_script` 留待改 `uscript_getShortName+hb_script_from_string`)。
- configure 修的坑:gperf 加 PATH;`SQLite3::SQLite3` 这版 FindSQLite3 没建 target → 显式补建;`OpenGL::GLES` 无条件被链 → 建空 INTERFACE target;`USE_LCMS/USE_JPEGXL/USE_AVIF` 默认 ON → 关掉。
- **🎉 `cmake configure` 退出 0**(build dir `build-clang-webcore`,脚本 `configure-phase1.ps1`)。

### 🔨 S7 WebCore 编译-修复循环 — 进行中(后台 `ninja WebCore`)
WTF + JSC **编译通过**(Phase 0 复现,WTF.lib/JavaScriptCore.lib 在 build-clang-webcore);WebCore **代码生成全部成功**(数百 IDL binding);进入 WebCore 对象编译(~570 文件),逐个 App Container 补丁。这是 Phase 1 真正的大山(数百文件,多周量级),今晚尽量深推。
已修的构建环境/源码坑:
- **`PYTHONUTF8=1`**(+PYTHONIOENCODING):中文 Windows 上 Python 代码生成脚本默认 GBK 读 UTF-8 临时文件炸(inspector 协议 binding)。已加进 arm32-uwp-env.ps1。
- **`MSYS2_ARG_CONV_EXCL=* / MSYS_NO_PATHCONV=1`**:WebCore IDL 预处理用 Git 的 MSYS2 Perl,把 `/EP /nologo /TP` 等 MSVC flag 误转成 `C:/Program Files/Git/...` 路径(`preprocessor.pm:51` 走 Windows 分支)。已加进 env。
- **`FontMemoryResource.h`**:析构的 `RemoveFontMemResourceEx`(GDI 桌面 API)→ 分区宏守卫(走 FreeType 不创建此资源)。修好 8 个字体文件。
- **`wtf/win/GDIObject.h`**:`deleteObject<T>` 的 `::DeleteObject`/`::DeleteDC`(GDI)→ 分区宏守卫 no-op。修 FrameWin(`GDIObject<HBITMAP>` 拖拽图像)等。
- **`ComplexTextControllerHarfBuzz.cpp`**:`hb_icu_script_to_script`→`uscript_getShortName+hb_script_from_string`,去 `<hb-icu.h>`(不链 hb-icu)。
规律同 Phase 0:**"WebKit 假设 Windows=x86_64 桌面",ARM32+App Container 上 GDI/桌面专属 API 全崩,逐个换 App Container 等价物或 stub。** 量约 Phase 0 的 ~10×。

**🎉 IDL→JS binding 生成全线打通(今晚最硬卡点之一,1731 个 binding 全生成):**
- **Python 编码**:`PYTHONUTF8=1`(中文 Windows GBK 读 UTF-8 临时文件炸)。
- **IDL 预处理器形式**:`WebKitCompilerFlags.cmake` 的 `MSVC` 分支给 clang-cl 用 `/nologo /EP /TP`(`/EP` 输出到 stdout 且无 #line,是 IDLParser 要的干净形式;clang-cl 的 `-P` 会被当 cl 的 `/P` 写到文件→stdout 空,`-E -Wp,-P` 又带 #line)。
- **MSYS 路径转换**:Git 的 MSYS2 Perl(`$^O=msys`,osname 空)经 open2 跑预处理时,把 `/nologo /EP /TP` 误当 POSIX 路径转换成 `C:/Program Files/Git/nologo`。修:`arm32-uwp-env.ps1` 设 **精准** `MSYS2_ARG_CONV_EXCL="/nologo;/EP;/TP"`(只排这 3 个 flag,不排 fileName——否则 .idl 路径的 POSIX↔Windows 往返转换也被关掉变 `/e/...` 打不开)。
- **🔑 CRLF 大坑**:`included_idl_files_WebCoreBindings.tmp`(IDL 路径列表)是 CRLF,`CodeGenerator.pm` 的 `IDLFileForInterface` 用 `chomp`(只去 `\n`)→ 残留 `\r` 让 `fileparse($_,".idl")` 提出 `EventTarget.idl\r`(`.idl` 后缀匹配失败)→ 找不到 EventInit/EventTarget/GeolocationPosition 等**基础依赖接口**。修:`CodeGenerator.pm:577` 加 `$_ =~ s/\r$//`。**这一处修好,binding 从 14 → 1731 全生成。**

**WebCore 编译又一批 App Container 补丁:**
- `sqlite3-vcpkg-config.h`:installed 的 sqlite3.h 引它但之前没拷 → 拷进 installed/include,并把 `SQLITE_API __declspec(dllimport)` 改空(静态库,否则找 `__imp_sqlite3_*` 链不上)。
- `LocaleICU.cpp` / `ComplexTextControllerHarfBuzz.cpp`:`hb_icu_script_to_script` → `uscript_getShortName + hb_script_from_string`,去 `<hb-icu.h>`(harfbuzz 未编 hb-icu;全 WebCore 仅此 2 处非-Skia 用点)。
- `MainThreadSharedTimer.{h,cpp}`:`OS(WINDOWS)&&USE(FREETYPE)` 走 RunLoop 分支(真实计时器),排除 HWND/SetTimer 的 `MainThreadSharedTimerWin.cpp`。
- `Font.h` / `FontCustomPlatformData.h`:`PLATFORM(WIN)` 字体分支加 `&& !USE(FREETYPE)`,让 freetype 港落到 freetype 成员/构造(Uniscribe SCRIPT_CACHE、DirectWrite name-based 布局 → 改用 FcPattern/cairo_font_face/FT_Face)。
- `BString.cpp` 加 `<oleauto.h>`;排除 KeyEventWin/WindowsKeyNames(GetKeyState,输入暂缓)。

**进度:WebCore 对象编译 [78/607],随跨平台主体(DOM/CSS/render)推进,GDI 类坑变稀。**
### ⏸ S8 网络栈(curl+openssl+psl)— Phase 1b(WebCore 链接前必须;静态 HTML 渲染不需要其功能,但链接需符号)
### ⬜ S6 WebCore CMake 接线(PlatformWinUWP.cmake + FreeType.cmake 移植进 Win port + 最小特性)
### ⬜ S7 WebCore 编译-修复循环
### ⬜ S8 网络栈选型(WinHTTP vs curl+openssl)

## 已知风险/笔记
- **多份 freetype 实例**:freetype 是静态 lib,会被 fontconfig-1.dll 和 cairo-2.dll 各静链一份;WebKit 自己也链 freetype 并把 FT_Face 传给 cairo-ft → 三方必须同一个 freetype 运行时,否则 FT_Library 不匹配。链 WebKit 时需解决(理想:freetype 做成单一共享 DLL 大家共用)。
